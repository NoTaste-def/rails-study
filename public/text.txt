public:
이미지, css, javascript 파일과 같은 정적 파일들을 저장한다.
브라우저에서 직접 접근 가능한 파일들이다.

(에러처리 화면에도 쓰는 것 같다.)


이곳에 있는 png 파일 등은 image_tag 와 같은 메소드 없이도 url을 통해 직접 접근이 가능하다.

또한 assets pipeline을 거치지 않기 때문에 최적화나 버전관리가 자동으로 적용되지 않는다.